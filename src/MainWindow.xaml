<Window
    x:Class="NoReveal.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="No Reveal">

    <Grid>
        <Grid.Background>
            <SolidColorBrush Color="Transparent"/>
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Custom title bar host -->
        <Border x:Name="AppTitleBar"
                Grid.Row="0"
                Height="40"
                Padding="16,0"
                VerticalAlignment="Stretch"
                Background="Transparent"
                IsHitTestVisible="True">
            <TextBlock Text="No Reveal"
                       VerticalAlignment="Center"
                       FontSize="12"
                       FontWeight="SemiBold"
                       Opacity="0.8"/>
        </Border>

        <!-- Main Content -->
        <ScrollViewer x:Name="MainScrollViewer" Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Padding="24,8,24,24" Spacing="4" Background="Transparent" HorizontalAlignment="Stretch">

                <!-- Blocking Section Header -->
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8,8,0,0"
                        Padding="16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <FontIcon Glyph="&#xE8A7;" FontSize="20" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="16,0">
                            <TextBlock Text="Blocking" FontSize="14" FontWeight="SemiBold"/>
                            <TextBlock Text="Prevent reveal actions based on proximity" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>
                        <ToggleSwitch x:Name="EnabledToggle" Grid.Column="2" IsOn="True" OnContent="On" OffContent="Off" Margin="0,-8,0,-8" HorizontalAlignment="Right" MinWidth="0"/>
                    </Grid>
                </Border>

                <!-- Block Distance row -->
                <Border Background="{ThemeResource LayerFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1,0,1,1"
                        CornerRadius="0,0,8,8"
                        Padding="52,12,16,12"
                        Margin="0,-1,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center" Margin="0,0,16,0">
                            <TextBlock Text="Block distance" FontSize="14" FontWeight="SemiBold"/>
                            <TextBlock Text="Block distance actions based on proximity" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center" HorizontalAlignment="Right">
                            <NumberBox x:Name="DistanceBox" Value="2" Minimum="1" Maximum="100" Width="100" SpinButtonPlacementMode="Inline"/>
                            <TextBlock Text="pixels" VerticalAlignment="Center" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Block Edges Expander -->
                <Expander HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Margin="0,4,0,0" CornerRadius="8" IsExpanded="True">
                    <Expander.Header>
                        <Grid HorizontalAlignment="Stretch">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <FontIcon Glyph="&#xE7F4;" FontSize="20" VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="16,0">
                                <TextBlock Text="Block edges" FontSize="14" FontWeight="SemiBold"/>
                                <TextBlock Text="Restrict specific monitor edges" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            </StackPanel>
                        </Grid>
                    </Expander.Header>
                    <StackPanel Padding="16,8,16,16" Spacing="12">
                        <CheckBox x:Name="TopEdgeCheck" Content="Top" />
                        <CheckBox x:Name="BottomEdgeCheck" Content="Bottom" IsChecked="True" />
                        <CheckBox x:Name="LeftEdgeCheck" Content="Left" />
                        <CheckBox x:Name="RightEdgeCheck" Content="Right" />
                    </StackPanel>
                </Expander>

                <TextBlock Text="Behavior" FontSize="14" FontWeight="SemiBold" Margin="0,20,0,8"/>

                <!-- Run on Startup -->
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8,8,0,0"
                        Padding="16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <FontIcon Glyph="&#xE7E8;" FontSize="20" VerticalAlignment="Center"/>
                        <TextBlock Text="Launch on Windows startup" Grid.Column="1" FontSize="14" VerticalAlignment="Center" FontWeight="Medium" Margin="16,0"/>
                        <ToggleSwitch x:Name="StartWithWindowsToggle" Grid.Column="2" IsOn="False" OnContent="On" OffContent="Off" Margin="0,-8,0,-8" HorizontalAlignment="Right" MinWidth="0"/>
                    </Grid>
                </Border>

                <!-- Start Minimized -->
                <Border Background="{ThemeResource LayerFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1,0,1,0"
                        Padding="16"
                        Margin="0,-1,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <FontIcon Glyph="&#xE7B8;" FontSize="20" VerticalAlignment="Center"/>
                        <TextBlock Text="Start minimized in system tray" Grid.Column="1" FontSize="14" VerticalAlignment="Center" FontWeight="Medium" Margin="16,0"/>
                        <ToggleSwitch x:Name="StartMinimizedToggle" Grid.Column="2" IsOn="True" OnContent="On" OffContent="Off" Margin="0,-8,0,-8" HorizontalAlignment="Right" MinWidth="0"/>
                    </Grid>
                </Border>

                <!-- Show Notifications -->
                <Border Background="{ThemeResource LayerFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1,1,1,1"
                        CornerRadius="0,0,8,8"
                        Padding="16"
                        Margin="0,-1,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <FontIcon Glyph="&#xEA8F;" FontSize="20" VerticalAlignment="Center"/>
                        <TextBlock Text="Show notifications" Grid.Column="1" FontSize="14" VerticalAlignment="Center" FontWeight="Medium" Margin="16,0"/>
                        <ToggleSwitch x:Name="ShowNotificationsToggle" Grid.Column="2" IsOn="True" OnContent="On" OffContent="Off" Margin="0,-8,0,-8" HorizontalAlignment="Right" MinWidth="0"/>
                    </Grid>
                </Border>

            </StackPanel>
        </ScrollViewer>

        <!-- Footer -->
        <Border Grid.Row="2" Background="{ThemeResource LayerFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8" Padding="24,16">
                <Button x:Name="SaveButton" Content="Save" Width="100" Style="{ThemeResource AccentButtonStyle}" Click="SaveButton_Click"/>
                <Button x:Name="CancelButton" Content="Cancel" Width="100" Click="CancelButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>